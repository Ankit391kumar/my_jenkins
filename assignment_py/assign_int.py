#Assign an integer port = 8080 (representing a server port) to a variable and use the hex() method to convert it to a hexadecimal string for a network configuration report.
'''port = 8080
hex_port = hex(port)
print(f"server port: {port} (hex: {hex_port})")'''

#Assign an integer memory = 1024 (representing memory in MB) to a variable and use the bit_length() method to determine the number of bits required to represent it for a system optimization task.
'''memory = 1024
bit_space = memory.bit_length()
print(bit_space)'''
# Assign an integer error_code = 500 (representing an HTTP error code) to a variable and use a comparison operator to check if it equals 500 for a log verification script.
'''error_code = 500
if error_code == 500:
    print("error code 500 is internal server error")
else:
    print("error node not found")'''
# Assign two integers, cpu1 = 8 and cpu2 = 16 (representing CPU cores), to variables and use a comparison operator to check if cpu1 is less than or equal to cpu2 for a resource allocation check.    
'''cpu1 = 8
cpu2 = 16
if cpu1 <= cpu2:
    print("Resource allocation check passed: cpu1 has less or equal cores than cpu2.")
else:
   print("Resource allocation check failed: cpu1 has more cores than cpu2.") 
'''

