#tuples ia similler th list but with a crucial fiffrence it id immutable
#we cann't modify the content of touple

'''data = (12, 13, 14)
print(type(data))'''

# if we want to update the content in the touple 
# first we want to convert tuples into list
# then we can modify the list and again
# we need to convert list into touple
'''data = (1, 2, 3)  # this is tuple
data = list(data) # convert tuple into list
data.append(4) # append list
data = tuple(data)# convert list into tuple
print(data) '''

